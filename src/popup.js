// Auth Notes — popup entry point
console.log("[auth-notes] popup loaded");
const btn = document.getElementById("settings-btn");
btn?.addEventListener("click", () => alert("Settings coming soon — see ROADMAP.md"));
